# tests/e2e

End-to-end tests that bring up the entire Docker Compose stack and exercise
real auth flows over real network sockets and real SVIDs.

> **Status:** Placeholder. The first e2e test —
> `test_full_auth_flow.py` — lands in Phase 3b and becomes the pinned
> invariant test for every subsequent change.
