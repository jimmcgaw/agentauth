# tests/integration

Cross-component integration tests. Each test exercises a small slice of the
stack — typically two or three components — without requiring the full
Docker Compose deployment.

> **Status:** Placeholder. First integration tests arrive with Phase 1b
> (Python SVID client) and Phase 2 (policy tests run via OPA).
