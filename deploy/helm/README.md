# deploy/helm — Helm charts

> **Status:** Placeholder. Helm charts may be added once the raw Kubernetes
> manifests under `../k8s/` stabilize. Whether we ship Helm charts at all is
> an open question — the alternative is Kustomize overlays. This decision
> will be captured in an ADR before any chart work begins.
